/**
 * Stateless utility / helper classes.
 *
 * <p>Examples: date formatters, string sanitizers, currency converters,
 * pagination helpers.  These are plain Java classes (no Spring beans),
 * so they can be tested without a Spring context.
 * Spring-managed helpers that need injection belong in {@code service} instead.
 */
package com.restaurant.rms.util;
