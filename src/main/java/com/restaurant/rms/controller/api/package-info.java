/**
 * REST API controllers consumed by JavaScript fetch / WebSocket clients.
 * All endpoints are prefixed /api/v1/** and return JSON (application/json).
 * Documented via SpringDoc (@Tag, @Operation, @ApiResponse).
 */
package com.restaurant.rms.controller.api;
