/**
 * Admin-only controllers: user management, role assignment, system configuration.
 * All mappings under /admin/** are restricted to ROLE_ADMIN in SecurityConfig.
 */
package com.restaurant.rms.controller.admin;
