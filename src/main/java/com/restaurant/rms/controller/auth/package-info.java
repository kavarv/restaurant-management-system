/**
 * Authentication controllers: login page, logout, registration, password reset.
 * These endpoints are whitelisted in SecurityConfig (no authentication required).
 */
package com.restaurant.rms.controller.auth;
