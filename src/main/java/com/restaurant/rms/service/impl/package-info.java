/**
 * Concrete service implementations annotated with {@code @Service}.
 * Each class implements the corresponding interface from the parent package.
 * Transaction boundaries ({@code @Transactional}) are declared here, not on the interface.
 */
package com.restaurant.rms.service.impl;
